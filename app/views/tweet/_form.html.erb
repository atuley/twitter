<%= form_for(@tweet, url: tweet_new_path) do |f| %>
  <%= f.label :content, 'What is on your mind?' %>
  <%= f.text_area :content, class: 'form-control' %>
  <div class="row">
    <%= f.button 'Submit', class: 'u-margin-left-sm u-margin-top-sm btn btn-primary' %>
  </div>
<% end %>
